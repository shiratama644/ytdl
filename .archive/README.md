# .archive/ — アーカイブ文書（参照のみ・変更禁止）

本ディレクトリは **cod-web**（別プロジェクト = ブラウザ向けマルチタイプ・ゲームプラットフォーム）
の文書を退避したものです。本リポジトリ（ytdl）の雛形元として受け継がれたものを
**参考資料としてのみ**保持しています。

- **ytdl の設計根拠には使わない**。`docs/` や作業中の根拠として引用しないこと。
- `cod-web-docs/` = cod-web の `docs/` を退避時点の姿で保存（arch / planning / research / task-list 等）。
- `.agent/logs/` にも cod-web 由来のログ（2026-09-03〜09）が存在 = 同様に参照のみ・書き換え禁止
  （AGENTS.md §8.5）。
- ドキュメント整理・一括置換の際は**本ディレクトリを射程外にする**（AGENTS.md §4.6）。
  退避解除・削除はユーザーの明示指示がある場合のみ。
